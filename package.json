{
  "name": "recordatorios-app",
  "version": "1.0.0",
  "description": "Aplicacion de recordatorios para clases y trabajos",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install --prefix frontend && npm run build --prefix frontend",
    "install": "cd backend && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "nodemailer": "^6.9.13",
    "pg": "^8.11.5",
    "sqlite3": "^5.1.7",
    "web-push": "^3.6.7",
    "resend": "^6.25.0"
  }
}
